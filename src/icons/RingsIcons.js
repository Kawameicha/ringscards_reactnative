import { createIconSetFromIcoMoon } from 'react-native-vector-icons';

import ringsIconConfig from './ringsicons-config.json';

const RingsIcons = createIconSetFromIcoMoon(ringsIconConfig);

export default RingsIcons;